[project]
name = "tennis-prediction"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "K4mp47", email = "campagnoloalberto5@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.3",
    "scikit-learn>=1.9.0",
]

[project.scripts]
tennis-prediction = "tennis_prediction:main"

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
    "openpyxl>=3.1.5",
    "pytest>=9.1.1",
    "requests>=2.34.2",
    "ruff>=0.15.21",
]
